# JARVIS 项目协作规则

本文件约束本仓库中的分析、讨论、文档与实现。项目名称可以保留，但架构说明统一使用“用户、主 Agent、Planner、Executor、Reviewer、Researcher”等功能术语；不得用影视梗、协议绰号或类比代替工程含义。

## 1. 主 Agent 负责组织问题

用户提供的是目标、材料和关注点，不是最终目录。主 Agent 必须主动完成抽象、归类和排序，不得等用户先把逻辑梳理好，也不得把用户的编号机械改写成同名章节。

动笔前先在内部完成：

1. 找出这次真正需要回答的核心问题或决定；
2. 区分已确认事实、候选假设、待决定事项和后续行动；
3. 合并同类内容，识别前置条件、因果关系和反馈关系；
4. 选择能够形成连续推理的章节顺序，再展开细节。

如果用户同时问模型、人数、推理强度和价格，应先识别它们都属于“资源配置”，再按能力门槛、团队拓扑、计算预算和成本约束分层，而不是平铺成四个互不相连的章节。

## 2. 叙事必须形成一条因果主线

项目级说明默认采用以下阅读路径：

```text
为什么要解决
→ 准备用什么思路解决
→ 这个思路为什么能够工作
→ 需要哪些相互依赖的模块
→ 当前确认了什么、尚未确认什么
→ 下一步为何是当前这一步
```

不同文档可按目的调整骨架：

- README：具体问题场景 → 监督矛盾 → 项目洞察 → 一次任务如何运行 → 实现基础 → 当前状态；
- 架构文档：设计压力 → 总体机制 → 模块关系 → 技术边界 → 已确认与待决定；
- 实施计划：目标与当前位置 → 阶段依赖 → 里程碑 → 退出条件 → 当前动作；
- 设计文档：本阶段为何存在 → 解决什么 → 如何工作 → 边界与风险 → 如何验收；
- Review：总体结论 → 按根因归类的问题 → 证据与影响 → 是否可推进。

章节顺序服务于理解和决策，不复现聊天发生的顺序。

“结论先行”作用于整篇文档和主要章节，不要求每个段落都先抛出一句结论。章节内部应允许读者经历“场景 → 矛盾 → 原因 → 机制 → 含义”的推导过程；如果连续多个段落都像可以单独摘出的结论卡片，应合并段落并恢复叙事连接。

## 3. 章节之间必须说明“为什么接下来谈这个”

每个主要章节都要能够回答：

1. 它承接了上一节留下的哪个问题？
2. 它解决后，为什么自然需要下一节？

过渡必须提供因果信息。例如：

> 技术可行性只能证明系统能够被实现，还不能决定各角色拥有什么权力，因此下一步先讨论治理边界。

不要用“另外”“接下来介绍”“还有一点”等无信息连接词拼接孤立内容。如果两个章节可以任意调换而不影响理解，应重新检查它们是否缺少依赖关系，或是否本应合并。

## 4. 同类合并，层级展开

- 同级标题必须处于相近抽象层次；
- 一个上位主题至少包含两个真正并列的子主题；
- 平铺列表超过五项时，优先重新分类；
- 表格用于比较，列表用于契约，流程图用于关系；因果解释必须由正文承担；
- 非平凡章节至少用一个具体任务、失败场景或数据流贯穿解释，让读者看到结论怎样产生；
- 不把每一个判断单独升级成标题，也不让每段只承担一句抽象判断；
- 先给 Overview，再按依赖关系进入 Details。

## 5. 主动暴露缺口，不用细节掩盖未决问题

尚未讨论的模块只能标为候选问题，不得写成已冻结协议。遇到前提缺失时，主 Agent应指出缺失的决定及其影响，并把它放到正确的讨论顺序中。

所有重要内容使用以下成熟度之一：

- `Confirmed`：已经明确同意，可以约束后续设计；
- `Proposed`：有候选方案，但尚未接受；
- `Open`：尚未形成候选结论；
- `Verified`：已经由实现、测试或运行证据验证；
- `Superseded`：已被新决定替代，只保留历史用途。

文档权威性与内容成熟度是两条独立轴：

- `Active`：当前权威入口，可以同时包含明确标注的 Confirmed、Proposed 和 Open 内容；
- `Historical`：只用于追溯，不再驱动决定或实现。

`Active` 不等于全文已经确认，`Historical` 也不等于内容毫无价值。不得用含混的 `Draft / Active` 合并两种含义。

## 6. 文档各司其职

```text
README.md
└── 面向新读者：为什么做、是什么、现在到哪、从哪里继续

docs/ARCHITECTURE.md
└── 当前确认的总体机制、模块关系、技术边界与架构未决项

docs/PLAN.md
└── 唯一的阶段状态、里程碑、退出条件和当前下一步

docs/designs/<milestone>.md
└── 仅在里程碑获准进入设计后创建，记录该阶段的范围、接口和验收

docs/archive/
└── 被暂停或替代的提案；可供追溯，但不再驱动实现
```

`docs/README.md` 是文档地图，必须说明每份文档回答什么、是否权威、何时更新。

## 7. 文档随决定和实现同步演进

主 Agent在每次形成稳定结论或完成实现后，主动判断哪些权威文档需要同步，不等待用户逐份点名：

- 项目定位、公开能力或使用入口变化：更新 README；
- 模块关系、系统不变量或技术边界被确认：更新 Architecture；
- 当前阶段、里程碑状态或下一步变化：更新 Plan；
- 当前实现范围、接口或验收变化：更新活动设计文档。

候选想法可以进入 Active 文档的 `Proposed/Open` 区域，但不能写成当前规则。活动设计先以 `Proposed` 创建，用户接受后转为 `Confirmed` 并冻结范围，测试通过后转为 `Verified` 并链接证据；被替代后移入 `docs/archive/`，注明继任文档，不静默删除，也不继续维护。

同一个事实只允许一个权威来源。其他文档使用一句必要摘要加链接，不复制模型表、阈值、状态机或路线清单。

## 8. 交付前检查

提交回答或文档前，主 Agent与 Reviewer 检查：

- 开头是否先说明结论和主线；
- 目录是否由主 Agent主动重组，而非照抄用户问题；
- 各章节是否有明确的承接和引出关系；
- 段落是否共同推进一个场景或推理，而不是连续堆放结论卡片；
- 已确认、候选和未决事项是否被清楚区分；
- 当前状态是否只由 `docs/PLAN.md` 维护；
- 是否出现同一事实的多份独立副本；
- 新读者能否不阅读原始聊天就理解“目标、当前位置和下一步”。

任一项不满足时，先重构信息结构，再润色句子。
