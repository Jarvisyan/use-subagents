interface:
  display_name: "Use V4 Flash Worker"
  short_description: "Transport a native job to DeepSeek Flash"
  default_prompt: "Use $use-v4-flash-worker to stage and dispatch this already-scoped task through the installed plaintext Hook."
