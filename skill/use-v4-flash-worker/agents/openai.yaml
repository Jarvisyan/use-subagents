interface:
  display_name: "Use V4 Flash Worker"
  short_description: "Dispatch bounded Luna or explicit DeepSeek jobs with reliable handoff"
  default_prompt: "Use $use-v4-flash-worker to dispatch this already-scoped task to Luna by default, using the provider-aware plaintext handoff when required, or to DeepSeek only when backend is explicitly deepseek."
