interface:
  display_name: "Use V4 Flash Worker"
  short_description: "Dispatch bounded jobs to native Luna, with explicit DeepSeek fallback"
  default_prompt: "Use $use-v4-flash-worker to dispatch this already-scoped task to native Luna by default, or through the installed plaintext Hook only when backend is explicitly deepseek."
